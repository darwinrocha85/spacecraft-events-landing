# URL base de la API del backend spacecraftSystem (sin barra final)
# Local por defecto:
VITE_API_URL=http://localhost:8080/api

# URL base de la tienda de entradas (spacecraft-tickets-frontend) a la que este
# landing enlaza al hacer click en "Comprar entrada" (sin barra final)
VITE_TICKETS_URL=http://localhost:5174
